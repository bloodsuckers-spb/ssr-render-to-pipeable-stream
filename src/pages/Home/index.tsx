import { SearchBar } from '../../components';

import styles from './index.module.css';

const Home = () => (
  <main className={styles.main}>
    HomePage
    <SearchBar />
  </main>
);

export default Home;
